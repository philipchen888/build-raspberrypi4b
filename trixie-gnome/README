Build trixie-gnome sdcard for raspberrypi4b:

Input files: ../rootfs-trixie/linaro-esp.img
             ../rootfs-trixie/linaro-rootfs.img

cd ~/build-raspberrypi4b/trixie-gnome

Plugin sdcard/usb3 stick to PC.
sudo ./mksdcardimg.sh
sudo mount /dev/sda1 /mnt
sudo 7z x ~/Downloads/RPi4_UEFI_Firmware.zip -o/mnt && sync && sudo umount /mnt
Eject sdcard/usb3 stick from PC.

Plugin sdcard/usb3 stick to rpi4b.
Esc -> Device Manager -> Rpi -> Advanced -> 3GB ( disable ) -> F10 -> Boot Manager -> first UEFI or SDCARD.
Login:
Username: linaro
Password: linaro
Setup wifi. Disable power saving, auto suspend and screen saver.
sudo systemctl start ssh.service
sudo systemctl enable ssh.service
uname -a
lsb_release -a
sudo ifconfig
sudo dpkg-reconfigure tzdata
sudo dpkg-reconfigure locales
date
sudo apt update
sudo apt upgrade
sudo apt autoremove
sync
free
sudo shutdown -h now

Power off, Power on rpi4b.
Check CPU speed:
cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_cur_freq

glxinfo | grep OpenGL
dpkg -l | grep mesa
date
Check if Debian is running UEFI mode:
ls /sys/firmware/efi
Check display mode: ( wayland or x11 )
echo $XDG_SESSION_TYPE
Check audio driver: ( pulseaudio or pipewire )
pactl info

sudo apt install speedtest-cli python3-pip python3-setuptools ffmpeg mpv glmark2-es2-wayland pandoc firefox-esr vlc vlc-plugin-video-output vsftpd

speedtest-cli --secure

glmark2-es2-wayland

aplay -l
pavucontrol  ( select hdmi as default )

firefox -> youtube.com -> search YmDNhi07_Ho

git clone https://github.com/yt-dlp/yt-dlp.git
cd yt-dlp
make
cd ../
sudo cp ~/yt-dlp/yt-dlp /usr/local/bin
v4l2-ctl --list-devices  ( look for bcm2835-codec-decode (platform:bcm2835-codec) )
mpv --hwdec=help         ( look for v4l2m2m-copy )
ffmpeg -hide_banner -encoders | grep v4l2  ( look for h264_v4l2m2m )
yt-dlp -f 'bestvideo[ext=mp4][protocol=https]+bestaudio[ext=m4a][protocol=https]' 3qrKjywjo7Q -o swan.mp4 --postprocessor-args " -c:v h264_v4l2m2m -b:v 2M -c:a copy"

mpv swan.mp4
ffplay -autoexit swan.mp4
cvlc -gl=wgl --glconv=any swan.mp4

Plugin ASUS USB-BT500 bluetooth usb dongle to rpi4b.
sudo rfkill list
hciconfig hci0
hcitool dev
hciconfig -a
